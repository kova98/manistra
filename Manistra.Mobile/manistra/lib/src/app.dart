import 'package:flutter/material.dart';
import 'package:manistra/screens/home.dart';

class App extends StatelessWidget {
  Widget build(context) {
    return MaterialApp(
      title: 'Manistra',
      home: Home(),
    );
  }
}
